# File: src/stamp/engine/dependency_graph.py

class DependencyGraph:
    """
    Minimal DependencyGraph Implementation
    --------------------------------------
    Does nothing in this version. Returns empty fatal error list.
    """

    def analyze(self, metadata: dict, repo_root: str) -> list:
        return []
